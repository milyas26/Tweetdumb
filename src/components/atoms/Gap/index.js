const Gap = ({ width, height, backgroundColor, borderBottom }) => {
  return (
    <div
      style={{
        width,
        height,
        backgroundColor,
        borderBottom,
      }}
    />
  );
};

export default Gap;
